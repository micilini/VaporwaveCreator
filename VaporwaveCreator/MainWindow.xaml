<Window x:Class="VaporwaveCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VaporwaveCreator"
        xmlns:controls="clr-namespace:VaporwaveCreator.Views.Controls"
        mc:Ignorable="d"
        Title="ＶＡＰＯＲＷＡＶＥ ＣＲＥＡＴＯＲ"
        Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="40"
            ResizeBorderThickness="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Border CornerRadius="8" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#C471ED" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF6B9D" Offset="0.0"/>
                        <GradientStop Color="#C471ED" Offset="0.5"/>
                        <GradientStop Color="#12CBC4" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>

                <TextBlock Text="🌴 ＶＡＰＯＲＷＡＶＥ ＣＲＥＡＴＯＲ 🌴"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Foreground="White"
                           FontSize="13"
                           FontWeight="Bold"
                           WindowChrome.IsHitTestVisibleInChrome="False"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,8,0"
                            WindowChrome.IsHitTestVisibleInChrome="True">

                    <Button x:Name="BtnMinimize"
                            Width="30" Height="24"
                            Margin="2,0"
                            Click="BtnMinimize_Click"
                            ToolTip="Minimize"
                            Style="{StaticResource ChromeButtonStyle}">
                        <TextBlock Text="─" FontSize="11" Foreground="White" Margin="0,-3,0,0"/>
                    </Button>

                    <Button x:Name="BtnClose"
                            Width="30" Height="24"
                            Margin="2,0"
                            Click="BtnClose_Click"
                            ToolTip="Close"
                            Style="{StaticResource ChromeButtonCloseStyle}">
                        <TextBlock Text="✕" FontSize="11" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>

            <controls:WebViewControl x:Name="WebViewControl" Grid.Row="1"/>
        </Grid>
    </Border>
</Window>
